# Copie este arquivo para ".env" (mesma pasta) e ajuste a porta conforme
# o endereço HTTPS que o Visual Studio mostra ao rodar o backend
# (Properties/launchSettings.json do projeto BancoDados_Kasaleve_SistemaTudo).
REACT_APP_API_URL=https://localhost:7000
